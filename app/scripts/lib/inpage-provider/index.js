const MetaverseVMInpageProvider = require('./src/MetaverseVMInpageProvider')
const { initProvider, setGlobalProvider } = require('./src/initProvider')

module.exports = {
  MetaverseVMInpageProvider,
  initProvider,
  setGlobalProvider,
}
